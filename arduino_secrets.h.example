// Copy this file to arduino_secrets.h in each sketch folder and fill in your credentials.
// arduino_secrets.h is gitignored — credentials are never committed.
#define SECRET_SSID "your_network_ssid"
#define SECRET_PASS "your_network_password"
